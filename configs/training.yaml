engine: "mlx" # mlx or hf
iters: 500 # Reduced for demonstration speed
batch_size: 4
learning_rate: 1e-5
steps_per_report: 10
steps_per_eval: 100
resume_adapter_file: null
adapter_path: "llama-3.2-1b-adapters"
save_every: 100
lora_parameters:
  rank: 16
  alpha: 32
  dropout: 0.05
  target_modules: ["q_proj", "v_proj", "k_proj", "o_proj"]
wandb:
  enabled: false
  project: "llm-mac-fine-tune"
